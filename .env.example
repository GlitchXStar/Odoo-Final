# Database
DB_NAME=empay_hrms
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# Backend
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=24h
NODE_ENV=production

# SMTP (optional, for OTP/email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
